[package]
name = "vu-test"
edition.workspace = true
license.workspace = true
version.workspace = true
description = "Logic test framework for vu-cli — drives a live vu session via .test files"

[[bin]]
name = "vu-test"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }
serde_json = { workspace = true }
clap = { version = "4.5", features = ["derive"] }
